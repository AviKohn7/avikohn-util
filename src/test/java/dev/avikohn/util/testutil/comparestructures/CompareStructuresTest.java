package dev.avikohn.util.testutil.comparestructures;

import org.junit.jupiter.api.Test;

public class CompareStructuresTest{
    @Test
    public void testInnerClasses(){
        //ClassComparison<?,?> comparison = new ClassComparison<>(CompExample1.class, CompExample2.class).setAccessFlag(Flag.joinFlags(Flag.INSTANCE, Flag.DECLARED_AND_PUBLIC));
//        ConstructorComparer.GUESS_EXTRA_PARAMETER = true;
//        ClassComparison<?,?> comparison = new ClassComparison<>(CompExample1.class, CompExample2.class);
//        comparison.setAccessFlag(Flag.ALL);
//        comparison.compareAndPrint();
    }
}
